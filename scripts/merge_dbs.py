#!/usr/bin/env python

import argparse

def merge_total_cycles(dbs):

def main():
    parser = argparse.ArgumentParser()
    parser.add_argument("output_db")
    parser.add_argument("dbs", nargs="+")
    args = parser.parse_args()


if __name__ == "__main__":
    main()
